def descending_order(num):
    return int("".join(sorted(str(num),reverse = True)))
print(descending_order(42145))# - этот метод возвращает в списке цифр самое большое и записывает его по убыванию
